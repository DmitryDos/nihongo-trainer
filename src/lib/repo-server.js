// Серверная точка входа для API-роутов: сайд-эффект открывает БД (node:sqlite)
// и регистрирует handle через setDb, после чего re-export отдаёт весь repo.
// Роуты импортируют repo отсюда (а не из "./repo.js"), чтобы гарантированно
// инициализировать серверный драйвер до первого запроса.
import "./db.js";
export * from "./repo.js";
